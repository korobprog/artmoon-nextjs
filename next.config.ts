/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {},
  // Можно также настроить глобальное поведение прокрутки в Next.js
  // Например:
  // scrollRestoration: false, // Отключает автоматическое восстановление позиции прокрутки
};

export default nextConfig;
